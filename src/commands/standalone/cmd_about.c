#include <stdio.h>

int main() {
printf("SegShell is a minimalistic shell I created for fun.\n\
What makes it different from other shells (bash, sh, zsh, etc.) is that it has very few\nbuilt-in commands — most commands live as standalone binaries in /mybin.\n");

}